<Application x:Class="Bykova41p_pr5.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Bykova41p_pr5"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        
        <ControlTemplate x:Key="CTButon" TargetType="Button">
            <Border CornerRadius="25,10,15,10" BorderBrush="Black" BorderThickness="2"
                    Background="ForestGreen" Width="250" >
                <ContentControl Content="{TemplateBinding Content}" HorizontalAlignment="Center" Margin="10" Foreground ="White"/>
            </Border>
        </ControlTemplate>


        <Style x:Key="TX"  TargetType ="TextBlock">
            <Setter Property="Foreground" Value="Green"/>
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
        
        <Style x:Key="CB"  TargetType ="ComboBox">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="80,45"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Width" Value="250"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>

    </Application.Resources>
</Application>
